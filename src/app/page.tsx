import GamedayReport from '@/components/GamedayReport';
import { getLatestGamedayReport } from '@/shared/gameday-api';

// prevent this page from being statically generated by vercel build
export const dynamic = 'force-dynamic';

// TODO: bunch of items...
// - start tracking ops
// - legend explaining trending icons and fire icon
// - collapsible sections
// - player first names
// - pitcher handedness
// - pitcher avg strikeouts?
// - AI summary section for how Jays have performed lately and how they did against the current opponent historically
// - remove redundant team abbreviations

export default async function Index() {
  const report = await getLatestGamedayReport();

  return <GamedayReport report={report} />;
}
